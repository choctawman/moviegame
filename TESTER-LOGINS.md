# Tester Logins

Fresh installs restore the bundled shared league snapshot and reset these accounts to the shared password:

- Password for every account: `moviegame123`

Accounts:

- `commissioner@example.com` — Lee — Commissioner Team
- `opening.weekend@example.com` — Blake — Opening Weekend
- `box.office@example.com` — Travis — Box Office Bombers
- `critics.cut@example.com` — Maggie — Critics Cut
- `silver.screen@example.com` — Emily — Silver Screen
- `lucky.losers@example.com` — Mitch — Lucky Losers
